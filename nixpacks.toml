[phases.setup]
aptPkgs = ['curl', 'gcc', 'g++', 'libpq-dev']

[phases.install]
cmds = [
  'cd backend',
  'pip install -r requirements.txt'
]

[phases.build]
cmds = [
  'cd backend',
  'python -m alembic upgrade head'
]

[start]
cmd = '/app/start.sh'

[variables]
PORT = '8000'